name: myfistworkflow
on:
  push:
    branches: 
      -main
  workflow_dispatch:
jobs:
  first_job:
    name: my first job
    runs-on: ubuntu-latest
    steps:
      - name: Greet the user
        run:  echo "Hello world"
        shell: bash
   
  
    
    
  
    
    
    
      
      
    


